# 讲解 Loop 02：图解卷积视觉分块与类比插图

## 目标

在最新 main 与已验收 Loop 01 上，精简 2.4-2 概览、分块课程正文、提高阅读字号，并接入两张已批准的 V2 类比插图进行桌面/移动真实页面验收。

## 需求

- 以 `origin/main@37360f7` 为基线，保留 Loop 06 的课程缓存简化结构，并迁入已验收的 Loop 01 教学内容与 Figure 2.7 幅值修正。
- Section objective 改成一句可以快速扫读的目标；Concepts in this section 只保留 3 个一级知识点，不再把动作拆成六项。
- 保留 Why -> What -> How -> 五步总结 -> GeoGebra -> 本书用途的六段教学顺序和已经确认的原话，不重写核心教学逻辑。
- Why 页在墨水池类比旁使用 `convolution-ink-memory-v2.png`；How 页在洒水车类比旁使用 `convolution-sprinkler-procedure-v2.png`。
- 桌面端采用“正文为主、插图为辅”的并排布局；移动端改为上下布局。两张 9:16 插图必须完整显示，不得裁掉关键构图，也不得铺满整页。
- 每页用少量语义分块帮助扫读，避免整页只有连续段落；不得增加卡片套卡片、装饰性大标题或与教学无关的说明文字。
- 仅对本小节提高正文可读性，不全局放大其他课程，也不修改稳定的 GeoGebra 运行时、场景协议或课程分页架构。
- 新插图必须通过 `workspace/materials/` 下的受限静态路由提供；禁止复制到 `app/` 或改用外部 URL。
- 旧写实插图保留为本轮生成历史，但课程和测试只能引用 V2 文件。
- GeoGebra 继续是本节唯一主交互视觉；插图只承担类比记忆，不替代 Figure 2.7 Demo。

## 验收标准

- [ ] 最新 `main` 的 Loop 06 结构仍在，当前分支只叠加 Loop 01 与本轮范围内修改。
- [ ] 真实 `/api/section` 命中目标缓存；概览为 1 句目标、3 个一级知识点，正文仍为 6 个 H2 和 1 个 GeoGebra Demo。
- [ ] 课程只引用两张 V2 插图，两个 URL 返回 `200 image/png`；路径穿越请求被拒绝或返回 404。
- [ ] 桌面端两张类比页为文字与小幅插图并排，图片不超过正文视觉权重；移动端图片位于文字下方且页面无横向溢出。
- [ ] 插图完整可见，无拉伸、裁切、空白占位或加载失败；懒加载后布局稳定。
- [ ] 正文比第一版略大且更易读，但标题、公式、列表、类比图和分页控件不重叠。
- [ ] Figure 2.7 的四步、`t=-3/-2/0` 数值和移动端讲义/问答状态保留全部通过。
- [ ] 真实页面单独判断洒水车笑脸是否偏幼稚；如果其余构图有效，只定向重做洒水车，不连带重做墨水池。
- [ ] 定向契约、静态路由、`npm run check`、CSS probe 与视觉/桌面/移动验收通过。
- [ ] `verification.md` 先写中文完整记录，再写英文摘要。

## 范围外

- 不继续生成第三版图片，除非真实页面证明确有单点问题。
- 不修改 GeoGebra 数学、课程问答、用户偏好、RAGFlow 或其他章节缓存。
- 不把本轮版式抽象成全站通用课程组件；第二个章节需要同类版式时再评估抽象。
