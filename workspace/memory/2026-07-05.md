# 2026-07-05 — Post-refactor housekeeping sweep

Refactor closed 2026-07-04 (`docs/REFACTOR_DONE.md` top banner). This session was a
docs/memory reconciliation pass — a 9-agent audit compared every supporting document
against the actual repo state (52 findings), then fixes landed direct to main.

## What changed

- **CLAUDE.md**: architecture line counts refreshed (app.js ~19k→~5.7k, ws-bridge
  ~6.7k→~5.3k); "three package.json versions" → single root package.json.
- **README.md**: Main Directories now lists `workspace/materials/` (root `materials/`
  removed 2026-07-05), sync-policy pointer reworded.
- **`.trellis/spec/app/`** (index/conventions/architecture): materials mirror language
  replaced with single-tree policy; `npm run check` scope corrected to 53 files;
  recrops constraint paths updated; package.json count fixed.
- **docs/**: `phase3_plan.md` + `PHASE3_PREP.md` got SUPERSEDED banners;
  `B4_LESSON_RENDER_PLAN.md` status corrected from "NOT YET EXECUTED" to shipped
  (#116); `HARNESS_INTERACTIVE_PLAN.md` status corrected to shipped (#112/#113/#115);
  `REFACTOR_DONE.md` DONE banner gained final 2026-07-05 metrics
  (style.css 32,514 / app.js 5,741 / ws-bridge 5,348 / index.html 1,641 / rcc 1,562);
  `REFACTOR_PLAN.md` deltas table marked historical; `WINDOWS_RAG_HANDOFF.md`
  materials paths moved to `workspace/materials/`.
- **workspace/memory/**: new `codebase-map-2026-07-05.md` (current headline numbers +
  module inventory); 06-19/06-20 maps banner-marked superseded (06-19 subsystem
  contracts remain the deep reference).
- External stores updated in the same session: Claude memory store (phase3 status
  condensed to closed-state; codebase-map pointer refreshed) and central DB
  (`projects/aquarius-tutor` current-state + decisions log entry for the close-out).

## Left as-is (deliberate)

- `docs/sync-policy.md` — its 2026-07-05 header already frames the two-tree sections
  below as historical.
- Dated snapshot tables inside REFACTOR_DONE/REFACTOR_PLAN — historical record;
  banner tells readers to re-grep.
- central DB `knowledge/aquarius-ui-testing-strategy.md` — still `last_verified
  2026-06-20`; re-verify next time the harness is touched rather than bump the date
  without re-verification.

## 2026-07-05 (afternoon) — Phase 4 DB + login/session planning (Trellis task created)
- Confirmed the deferred "Phase 4 DB migration" is for account/user data (user memory, chat sessions, feedback board) — `docs/phase3_deferred.md §1`.
- Created Trellis task `.trellis/tasks/07-05-user-db-auth-sessions/` with full planning artifacts (prd.md D1–D7 + R1–R4 + AC1–AC7, design.md, implement.md 3-PR plan, 3 research files).
- Key decisions: keep Clerk (dev instance for now, production+domain = backlog), Bearer-token verification server-side (JWKS+RS256, no SDK), Neon free Postgres + `pg` as first sanctioned runtime dep, restore last view at lesson granularity, guests stay ephemeral & client-side only, LLM endpoints require sign-in, fresh start (no data migration).
- Root cause of "refresh → homepage": Clerk restores the session but `allowAuthNavigation=false` routes to `syncCurrentUserWithoutNavigation()`; last location never persisted; plus backend trusts bare uid params (no auth at all); plus ephemeral Render FS.
- Status: planning complete, awaiting owner review of artifacts before `task.py start`. No code changed this session.

## 2026-07-05 (evening) — AFK loop: Stage A + Stage B shipped (PR #131, #132)
- Owner approved start; `task.py start` → in_progress. Planning artifacts committed to main (3bfa1e4).
- **PR #131 (Stage A backend, merged 2746f36)**: app/clerk-verify.js (JWKS+RS256, DI'd httpRequestJson, conditional azp, throttled unknown-kid refetch, warmUp on enforcement), app/db.js (Neon pg store: JSONB mirrors file shapes, uid gate, guest_* refusal, 64KB caps, ensureUserRow, multi-row feedback INSERT, honest boolean write results), user-memory.js dual backend (8-primitive store, public surface async), ws-bridge guards (TUTOR_REQUIRE_AUTH gate + opportunistic token verification — token sub ALWAYS beats client uid), /api/section cache-only mode gate for unauthenticated, CORS origin-reflect allowlist + always Vary: Origin on /api/*, feedback mutation serialization (in-process promise chain), storage init before listen. `pg` ^8.16 = first sanctioned runtime dep. New hermetic tools/test-auth-guard.js (43 asserts, 2 bridge spawns, local JWKS, invalid OPENROUTER key blocks app/.env's real key → zero spend).
- **PR #132 (Stage B frontend, merged 6e3c7ae)**: app/api-client.js apiFetch + getAuthToken (fresh 60s Clerk JWT per request), ALL uid params/body fields dropped (getUid deleted), last-location restore (localStorage `aquarius-last-location`, BOOT_SAVED_LOCATION script-eval snapshot defeats boot-path showWelcome pollution, live-key guard against yanking manual nav, parent-overview context reconstructed for back button), intro only for signed-out (session-hint `aquarius-had-session` self-heals via Clerk no-user listener + live-guest check), guest fully client-side (`aquarius-guest-memory` sessionStorage, rehydrateGuestSession on reload, real sign-in clears guest keys), graceful guest 401s. v1.5.0, cache busters v=1700. New tools/test-session-restore.js e2e (reuses test-utils helpers; asserts reload-restores-lesson + back-context + fresh-visitor intro control).
- Both PRs went through 8-angle sub-agent self-review + fixes (PR-A: 2 fix commits incl. write-status honesty + feedback race; PR-B: 12 findings fixed). Boot-state-machine bugs found by the new e2e matched the 2026-05-08 saga's predicted classes exactly.
- Render auto-deployed Stage A (verified: Authorization/DELETE/Vary markers on /health; legacy uid-param memory GET still 200; behavior unchanged with no env vars set).
- **Remaining (owner-gated, in order per design §7)**: (1) ~~real-Neon smoke~~ DONE (see below), (2) set DATABASE_URL on Render (durability flip; verify `SELECT count(*) FROM users WHERE uid LIKE 'guest_%'` = 0), (3) PR-C: set TUTOR_REQUIRE_AUTH=1 + CLERK_* on Render, prod AC4/AC5/AC6/AC3 verification, docs updates (CLAUDE.md pg exception + env vars, spec checklist, multi-session.md, phase3_deferred §1 shipped), file D7 follow-up (custom domain + Clerk production instance) to backlog. AC2 (browser restart ≤7d) Chromium-only per prd scoping.

## 2026-07-05 (night) — Real-Neon smoke PASSED (Stage A gate item closed)
- Owner created the Neon DB and provided DATABASE_URL (in `app/.env`, currently COMMENTED OUT so local dev stays on the file store; uncomment to point local at Neon; same string goes into Render's dashboard for the durability flip).
- Verified end-to-end against the live Neon instance: `[db] Postgres store active` boot (DDL bootstrap + TLS — node-postgres treats `sslmode=require` as verify-full, so the connection is fully certificate-verified); quiz save → `users`+`user_memory` rows; read-back with quiz normalization; feedback post → `feedback_items` row; real `/api/ask` → answer + `chat_sessions` row (2 messages, correct meta); **guest write → honest 500 + zero `guest_%` rows** (AC6 backstop live); process restart → memory + session survive (local analog of AC3); session GET/DELETE roundtrip. Smoke rows cleaned — DB left at 0 rows in all four tables.
- Ops note: `pkill -f "node app/ws-bridge.js"` inside a compound bash command kills the calling shell too (pattern matches the shell's own cmdline). Track `$!` and `kill $BPID` instead.
