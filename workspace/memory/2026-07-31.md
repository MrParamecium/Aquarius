# 2026-07-31

- 用户确认启动 `2.4-2 Graphical Understanding of Convolution Operation` 的讲解标准化 Loop 01，先完成可用第一版，之后按真实学习体验继续迭代。
- 第一版固定采用 Why -> What -> How -> 五步总结 -> Figure 2.7 GeoGebra -> 本书用途的“三层桥接”路线。
- 固定两个口语比喻：透明水池中分批滴入墨水用于解释历史贡献叠加；农田与移动洒水车用于解释翻转、平移、逐点相乘和积分。用户明确要求这两个比喻不配图，只靠自然语言帮助想象。
- 教材 Figure 2.7 不在正常课程流中展示静态图，改由一个连续 GeoGebra Demo 承载翻转、平移、重叠面积和输出曲线；失败降级仍可保留静态材料与公式。
- PDF 核对发现教材 Figure 2.7 的 `g(-2)` 高度为 `2`，正确输出是 `2[1-exp(-(t+3))]`（`t>-3`）；现有缓存和 GeoGebra 使用了高度 `1` 的归一化版本。该差异属于本 Loop 必须修正的教材数学对齐问题。
- “运用在哪里”必须关联本书：第 2.4 节 LTIC 零状态响应、Example 2.9 RLC 电路、教材明确提到的采样与滤波，以及 2.4-3 级联系统的 `h_1*h_2`。
- GitHub API 确认远端 `main` 为 `d57ecaf`。已在干净工作区创建分支 `codex/lesson-loop-01-convolution-explanation`，未触碰 `Fourier-tier-3` 中的未提交修改。
- 当前只完成并自检 `.trellis/tasks/07-31-lesson-loop-01-convolution-explanation/` 下的 PRD 与设计；尚未修改课程缓存或 GeoGebra 代码。下一步是用户复核书面规格，批准后再写实施计划并进入实现。
