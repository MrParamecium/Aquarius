# 2026-08-02

- 用户确认 2.4-2 第四版采用三阶段结构：`章节简介 | 正式讲解 | 练习巩固`。现有解析器仍输出 `1 + 6 + 1` 共 8 页，不新增第二套分页系统。
- 正式讲解固定为 What → Why → How → How → Figure 2.7 GeoGebra → 本章用途。返回正式讲解时恢复本次会话最近阅读位置；底部分页器改为阶段语义。
- 第一页按教材纸质页 178 的引入逻辑组织为“你已经知道、这一节解决什么、学习路线”，不再使用旧 Section Objective / Concepts 大卡片。
- 普通知识块使用轻量页边编号；只有真正有先后关系的翻转、平移、相乘、积分使用连续时间轴。旧大号编号卡不再输出。
- 墨水池、洒水车两张 V2 图、三个代码原生图解、唯一 GeoGebra 和 Figure 2.7 的教材数值全部保留。
- 2.4-2 页面切换使用约 `70ms` 提交和 `180ms` 锁定，重复实测约 `204–216ms`；删除纸张翻面效果，减少动画模式立即提交。其他课程继续使用原节奏。
- 桌面双栏下，简介三列会导致每行只有一两个词，因此默认改为纵向；只有讲义全宽且屏幕至少 1000px 时恢复三列。
- 390px 证据截图发现 Q&A “Show” 按钮遮挡第三个阶段标签。最终修复为在 2.4-2 移动端给阶段导航预留独立顶部空间，并在移动测试中增加矩形相交断言。经验：`scrollWidth === clientWidth` 只能证明无横向溢出，不能证明浮动控件没有遮挡。
- 自动化通过：课程静态契约、布局 `16/16`、GeoGebra `11/11`、移动面板 `7/7`、`npm run check`、16 个 CSS probe 状态和 32 个视觉回归视图。
- 真实页面在 `http://127.0.0.1:9160/` 运行。桌面 GeoGebra 正常绘制，浏览器无新增 JavaScript error，仅有项目原有 Tailwind CDN 提示。
- 分支为 `codex/lesson-loop-04-convolution-guided-flow`，基线为 `codex/lesson-loop-03-convolution-reading-structure`。按用户要求保持本地，不推送、不创建 PR、不合并。
